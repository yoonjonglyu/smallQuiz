import React from 'react';

import Layout from '../components/layout/layout';

const Quiz: React.FC = () => {
    return (
        <Layout header={"퀴즈"}>
            문제
        </Layout>
    );
}

export default Quiz;